# Shared
APP_NAME=minichat

# PostgreSQL
POSTGRES_DB=minichat
POSTGRES_USER=minichat
POSTGRES_PASSWORD=minichat
POSTGRES_PORT=5432

# Redis
REDIS_PORT=6379

# RabbitMQ
RABBITMQ_PORT=5672
RABBITMQ_MANAGEMENT_PORT=15672
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest

# API (Spring Boot)
API_PORT=8080
JWT_SECRET=change_me_in_prod_change_me_in_prod_change_me
JWT_EXPIRES_SECONDS=3600
DATABASE_URL=jdbc:postgresql://localhost:5432/minichat
REDIS_URL=redis://localhost:6379
RABBITMQ_URL=amqp://guest:guest@localhost:5672/
INFERENCE_BASE_URL=http://localhost:8000
INFERENCE_CONNECT_TIMEOUT_MS=5000
INFERENCE_READ_TIMEOUT_MS=600000
GEN_QPS_LIMIT=2
GEN_INFLIGHT_TTL_SECONDS=1800
GEN_DEFAULT_TEMPERATURE=0.7
GEN_DEFAULT_MAX_TOKENS=512

# Inference (FastAPI)
INFERENCE_PORT=8000
OPENAI_API_KEY=
DEFAULT_MODEL=gpt-4o-mini

# Worker (Go)
WORKER_LOG_LEVEL=info

# Web (Next.js)
WEB_PORT=3000
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
